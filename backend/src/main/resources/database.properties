# Configuración de conexión a SQL Server
# IMPORTANTE: no subir este archivo con credenciales reales a un repositorio
# público. Considera agregarlo a .gitignore y versionar solo un
# database.properties.example con valores de plantilla.

db.host=localhost
db.port=1433
db.name=NombreDeTuBaseDeDatos
db.user=tu_usuario
db.password=tu_password
